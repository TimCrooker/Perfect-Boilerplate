import { Section } from '@/components/core'
import styled from 'styled-components'

export const DatabasesWrapper = styled(Section)`
	overflow: hidden;

	.image {
		width: 100px;
		height: 100px;
	}
`
